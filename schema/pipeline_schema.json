{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Pipeline Execution Manifest",
  "type": "array",
  "items": {
    "type": "object",
    "required": [
      "order", 
      "repository_url", 
      "version_tag", 
      "config", 
      "setup_script"
    ],
    "properties": {
      "order": {
        "type": "integer",
        "description": "The number in the sequence of execution."
      },
      "repository_url": {
        "type": "string",
        "description": "The full Git URL for the simulator repository."
      },
      "version_tag": {
        "type": "string",
        "description": "Git branch, tag, or commit hash to checkout."
      },
      "config": {
        "type": "string",
        "description": "Path to the unified configuration file (the single source of truth - combined from config.json files from all modules in this pipeline)."
      },
      "setup_script": {
        "type": "string",
        "description": "The path to the shell script within this Library repository that provisions the environment."
      }
    }
  }
}